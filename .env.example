# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/social_media_db

# Server Configuration
NODE_ENV=development
PORT=5000

# PostgreSQL Connection Details (if using separate connection)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=social_media_db